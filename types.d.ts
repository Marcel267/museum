// import { Product } from "@prisma/client";

// type Product = Product & { count?: number };
type CardType = {
  src: StaticImageData;
  alt: string;
  desc: string;
  width?: number;
  height?: number;
};
